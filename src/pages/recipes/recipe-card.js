import { Chakra } from "chakra";
import {Container, Badge,  Link, List, ListItem, AspectRatio } from '@chakra-ui/react'
import { ExternalLinkIcon } from "@chakra-ui/icons";
// import { Title, WorkImage, Meta } from "components/recipes";
import P from '../../../components/paragraph'
import Layout from "components/layouts/article";
import { Title, WorkImage, Meta } from "components/recipe-card";

const RecipeCard = () => {
    return(
        <Layout title="Recipe-Card">
            <Container>
                <h1>hello</h1>
            </Container>
        </Layout>
    )
}

export default RecipeCard